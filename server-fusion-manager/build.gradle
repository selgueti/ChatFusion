plugins {
    id 'java'
}

dependencies {
    implementation project(':core')
}

jar {
    archiveBaseName = 'ServerFusionManager'
    archiveVersion = '0.1.0'
    manifest {
        attributes('Main-Class': 'fr.uge.net.chatFusion.ServerFusionManager')
    }
    from project(':core').sourceSets.main.output.classesDirs
}