plugins {
    id 'application'
}

dependencies {
    implementation project(':server')
    implementation project(':server-fusion-manager')
    implementation project(':client')
}

application {
    // Define the main class for the application.
    mainClass = 'chatFusion.App'
}